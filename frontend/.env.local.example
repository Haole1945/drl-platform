# Frontend Environment Variables
# Copy this file to .env.local and update values if needed

# API Base URL (for browser-side requests)
# Development: Use localhost when running frontend outside Docker
# Production: Use the actual domain or localhost if running in Docker
NEXT_PUBLIC_API_BASE=http://localhost:8080/api

# Example for production deployment:
# NEXT_PUBLIC_API_BASE=https://api.yourdomain.com/api
